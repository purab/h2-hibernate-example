# H2 database hibernate example


for building project run following command

mvn package


For deployment:
mvn tomcat7:deploy

Then to undeploy it:

mvn tomcat7:undeploy
Finally, to redeploy it after making changes:

mvn tomcat7:redeploy